# /run — Process a DAS site folder

Run the full validation pipeline on a site folder.

```bash
python code/main.py --site-folder $ARGUMENTS --ciq ./input/SFY0803A_MMBB_CIQ_EXPORT_20251118_173752.xlsx --output-dir ./outputs
```

If no arguments provided, default to the SFY0803A sample site.
