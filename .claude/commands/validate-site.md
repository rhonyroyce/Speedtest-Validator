# /validate-site — Dry run on 2 screenshots

Quick validation run that processes only 2 screenshots for testing.

```bash
python code/main.py --site-folder $ARGUMENTS --ciq ./input/SFY0803A_MMBB_CIQ_EXPORT_20251118_173752.xlsx --dry-run
```
